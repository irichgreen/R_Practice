## ------------------------------------------------------------------------
mat=matrix(sample(100,size=60,replace=TRUE),nrow=6)
mat[4,2]=NA
mat[2,7]=NA
mat

## ------------------------------------------------------------------------
x=c(1,3,5,NA)

## ------------------------------------------------------------------------
is.na(x)

## ------------------------------------------------------------------------
mean(x)

## ------------------------------------------------------------------------
x[is.na(x)]=mean(x,na.rm=T)
x

## ------------------------------------------------------------------------
repNA=function(x) {
       x[is.na(x)]=mean(x,na.rm=T)
       x
}

## ------------------------------------------------------------------------
x=c(1,3,5,NA)
repNA(x)

## ------------------------------------------------------------------------
mat=matrix(sample(100,size=60,replace=TRUE),nrow=6)
mat[4,2]=NA
mat[2,7]=NA
mat

## ------------------------------------------------------------------------
apply(mat,2,repNA)

## ----eval=FALSE----------------------------------------------------------
## function(x) { x[is.na(x)]=mean(x,na.rm=T);x}

## ------------------------------------------------------------------------
apply(mat,2,function(x) {x[is.na(x)]=mean(x,na.rm=T);x})  

## ------------------------------------------------------------------------
apply(mat,1,  function(x) { x[is.na(x)]=mean(x,na.rm=T);x})

