## ----include=FALSE,cache=FALSE-------------------------------------------
library(knitr)

opts_chunk$set(comment=NA, myCode=TRUE)
opts_template$set(myFig=list(fig.height=6,fig.width=6))
knit_hooks$set(
    myCode=function(before,options,envir){
        if(before){
            "\\footnotesize \\color{blue}"
        } else{
            "\\normalsize \\color{black}"
        }
    })

## ----myCode=TRUE---------------------------------------------------------
a=c(1,2,3)
b=c(2.1,"Kim")
c=c(a,b,b,a)

## ----myCode=TRUE---------------------------------------------------------
x=matrix(1:12,ncol=3);x

## ----myCode=TRUE---------------------------------------------------------
Height=c(173,154,160)
Weight=c(78,60,45)

## ------------------------------------------------------------------------
r=2              # 반지름
circle = pi*r^2  # 원주율 곱하기 반지름의 제곱
circle           # 원의 면적

## ------------------------------------------------------------------------
r==2  # r이 2와 같은가?

## ------------------------------------------------------------------------
r=3    # r 에 새로운 값 3 할당 
r==2   # r이 2와 같은가?

## ------------------------------------------------------------------------
r!=2   # r is not equal to 2 ? 

## ------------------------------------------------------------------------
a=b=c=d=2      #  a,b,c,d 모두에 한꺼번에 2 할당
a+b+c+d
a<-b<-c<-d<-3  #  a,b,c,d 모두에 한꺼번에 3 할당
a+b+c+d

## ------------------------------------------------------------------------
total=2680; n=31      # 짧은 두줄을 한줄로 줄여쓸때는 ; 를 사용한다 
average <- total/n    # 평균 = 총점/학생수
average               # 평균 인쇄

## ------------------------------------------------------------------------
5**2   # 5의 제곱
10%%3  # 10을 3으로 나눈 나머지
d=(1+2)*3
d
print(d)

## ------------------------------------------------------------------------
e=d-7
print(e)   # e와 print(e)는 동일하다.
e

## ------------------------------------------------------------------------
(e=d-7)  # e=d-7; e 두 줄 쓴 것과 동일

## ------------------------------------------------------------------------
a=c(1,2,3,10,6)   # a는 다섯 개의 숫자를 갖는 벡터가 된다.       
mode(a)           # a의 데이터의 종류는?
str(a)            # a의 구조는? 

## ------------------------------------------------------------------------
b=c(7,6)
a=c(a,0)          # a벡터에 기존의 a의 값에 0이라는 값을 추가한다. 
c=c(a,b,7,-5)     # c의 값에 a,b,7,-5 할당

## ------------------------------------------------------------------------
c      
length(c)         # c의 원소의 개수는?
c[2]              # 벡터 c의 두번째 원소의 값은?

## ------------------------------------------------------------------------
(b=1:20)   # b=1:20; print(b)와 동일하다. 

## ------------------------------------------------------------------------
seq(5)          # (1부터) 5까지 자연수의 벡터
seq(1,3,0.25)   # 1부터 3까지 0.25 간격으로 배열 만듦

## ------------------------------------------------------------------------
e=c(1,2,3)
f=rep(e,3)     # e 를 3회 반복
f

## ------------------------------------------------------------------------
a=c(1,2,3)
b=c(2.1,"Kim")
c=c(a,b,b,a)

## ------------------------------------------------------------------------
x=1
x
mode(x)
mode(x)="character"
x
y=as.numeric(x)-1
y
as.logical(y)

## ----warning=TRUE--------------------------------------------------------
a=as.numeric("Kim")
a

## ----error=TRUE----------------------------------------------------------
a=c()
a

## ----error=TRUE----------------------------------------------------------
a=c(1,2,3,NA,8,NA,10)    # a에 2개의 결측치(누락된 값)가 있다.
mean(a)
mean(a,na.rm=T)          # NA값을 제거(remove)하고 계산  

## ------------------------------------------------------------------------
sum(is.na(a))         # is.na() 함수의 값은 NA인 경우 1(즉 NA값의 개수를 센다).
a=c(a,NULL,8,NULL);a  # NULL을 추가해도 에러는 나지 않지만 변화가 없다.
(a=c(a,NA))           # NA를 추가하면 결측치 데이터 하나가 추가된다. 

## ------------------------------------------------------------------------
a=NULL              # a라는 객체 자체를 없애는 명령이다.        
a

## ------------------------------------------------------------------------
a=1:5
a=c(a,101,102)
b=c(a,103)
b

## ------------------------------------------------------------------------
Height=c(168,173,160,145,180)
Weight=c(80,65,92,53,76)
BMI=Weight/(Height/100)^2
BMI

## ------------------------------------------------------------------------
a=1:10
b=c(1,-1)
a+b

## ------------------------------------------------------------------------
b=10
a=c=2
b^2+c(1,-1)*4*a*c

## ------------------------------------------------------------------------
a=matrix(1:12,ncol=3) ;a

## ------------------------------------------------------------------------
a=matrix(1:12,ncol=3,byrow=T) ;a

## ------------------------------------------------------------------------
b=LETTERS[1:12];b       # 12개의 문자로 된 벡터를 만든다.
b=matrix(b,ncol=4);b    # 4개의 열을 가진 행렬로 만든다.

## ------------------------------------------------------------------------
b[3,2]           # 세 번째 행, 두 번째 열의 원소
b[2,4]           # 두 번째 행, 네 번째 열의 원소
b[2,]            # 두 번째 행 전체
b[,3]            # 세 번째 열 전체
length(b)        # 행렬 전체 원소의 개수
nrow(b);ncol(b)  # 행의 수;열의 수

## ------------------------------------------------------------------------
sex=c("Male","Female","Female","Male","Male")

## ------------------------------------------------------------------------
sex=factor(sex)
sex
str(sex)
levels(sex)
length(sex)

## ------------------------------------------------------------------------
smoking=c(1,1,2,3,1)
smoking=factor(smoking)
levels(smoking)=c("none","ex-smoker","smoker")
smoking

## ------------------------------------------------------------------------
mydata=data.frame(height=Height,weight=Weight,sex=sex,smoking=smoking)
mydata

## ------------------------------------------------------------------------
mydata[3,]
mydata[,1]

## ------------------------------------------------------------------------
mydata$height
mydata[["height"]]

## ------------------------------------------------------------------------
mydata[3]
mydata[[3]]

## ------------------------------------------------------------------------
str(mydata[3])
str(mydata[[3]])

## ------------------------------------------------------------------------
mydata$BMI=mydata$weight*10000/(mydata$height)^2
mydata

## ------------------------------------------------------------------------
str(mydata)
summary(mydata)

## ------------------------------------------------------------------------
plot(mydata)

## ------------------------------------------------------------------------
a=1:100
b=a[c(80:90)]
b

## ------------------------------------------------------------------------
a=c(1,2,3)
b=c(2.1,"Kim")
c=c(a,b,b,a)

## ------------------------------------------------------------------------
c[10]

## ------------------------------------------------------------------------
x=matrix(1:12,ncol=3);x

## ------------------------------------------------------------------------
x[1,2]   # 첫번째 행, 두번째 열 원소
x[1,]    # 첫번째 행 전부
x[,2]    # 두번째 열 전부 

## ------------------------------------------------------------------------
Height=c(173,154,160)
Weight=c(78,60,45)

## ------------------------------------------------------------------------
BMI=Weight*100^2/(Height^2)
BMI

